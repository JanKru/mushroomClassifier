import { Routes } from '@angular/router';
/** own created COMPONENTS 
import { MushroomClassifierComponent } from './mushroom-classifier.component';*/
/** own created OTHER 
import { AuthenticationGuard } from '../../shared/guards/authentication.guard';

export const mushroomClassifierRoutes: Routes = [
 {
    path: 'sign-in', component: MushroomClassifierComponent, canActivate: [AuthenticationGuard]
 }
];*/